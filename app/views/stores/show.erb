<h1><%= @store.name %></h1>

<!-- Create a link to the edit page to edit this stores name -->

<h1>Locations:</h1>
<!-- Iterate of all you @stores.locations and show their adresss. Then iterate over all your locations and show the products available at that location. -->
<ul class="ui list">

<% @stores.each do |store| %>
  <li class="ui item">
  <a href="/stores/<%= store.id %>"> <%= store.locations %>
    <% store.locations.each do |location| %>
    <%= location.address %>

      <% store.locations.each do |location| %>
        <%= location.product_locations.products.each do |product| %>
        <%= product.name %></a>
        </li>
        <% end %>
    <% end %>
  <% end %>
<% end %>

</ul>

